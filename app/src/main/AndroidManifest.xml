<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AIFitnessApp">

        <!-- Router — decides where to send the user on launch -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.onboarding.OnboardingActivity"
            android:exported="false"/>

        <activity
            android:name=".ui.dashboard.DashboardActivity"
            android:exported="false"/>

        <activity
            android:name=".ui.log.LogActivity"
            android:exported="false"/>

        <activity
            android:name=".ui.log.LogSummaryActivity"
            android:exported="false"/>

        <activity
            android:name=".ui.consistency.ConsistencyActivity"
            android:exported="false"/>

        <activity
            android:name=".ui.progress.ProgressActivity"
            android:exported="false"/>

        <activity
            android:name=".ui.workout.WorkoutHistoryActivity"
            android:exported="false"/>

        <activity
            android:name=".ui.habit.HabitTrackerActivity"
            android:exported="false"/>

        <activity
            android:name=".ui.settings.SettingsActivity"
            android:exported="false"/>

        <activity
            android:name=".ui.WorkoutPlanActivity"
            android:exported="false"/>

    </application>

</manifest>